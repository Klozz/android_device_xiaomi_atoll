#============= hal_vibrator_default ==============
allow hal_vibrator_default self:qipcrtr_socket create;
allow hal_vibrator_default sysfs:file { open read write };

# Vibrator calibration
allow hal_vibrator_default mnt_vendor_file:file { read open write };
